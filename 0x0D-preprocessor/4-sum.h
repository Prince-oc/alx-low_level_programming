#include <stdio.h>
#define SUM(x, y) ((x) + (y))

return:0
int main (void)
{
	int num1 = x
		int num2 = y
		int result = SUM(num1, num2);
	
	printf("absolute value of the  number %d is %d\n",number)

		return(0)
}
